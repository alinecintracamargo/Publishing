<%= form_with(model: part) do |form| %>
  <%= render "shared/error_messages", resource: form.object %>

  <div class="form-group">
    <%= form.label :part_number, class: "form-label" %>
    <%= form.text_field :part_number, class: "form-input" %>
  </div>


  <div class="form-group">
    <%= form.label :supplier_id, class: "form-label" %>
    <%= form.collection_select( :supplier_id, Supplier.all, :id, :supplier_name, {prompt: true}, {class: "form-input"}) %>
  </div>

  <div class="flex items-center justify-between flex-wrap">
    <div class="sm:flex-1 sm:mb-0 mb-6">
      <%= form.submit class: "btn btn-primary" %>
    </div>

    <% if form.object.new_record? %>
      <%= link_to "Cancel", parts_path, class: "btn btn-white" %>
    <% else %>
      <%= link_to "Cancel", part_path(@part), class: "btn btn-white" %>
    <% end %>
  </div>
<% end %>
